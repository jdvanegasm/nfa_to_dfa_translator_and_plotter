# nfa_to_dfa_translator_and_plotter
Desktop application built with Python and PySide6 that allows users to input a regular expression over {0,1}, automatically generates and visualizes the NFA, applies the subset construction algorithm from scratch to obtain the DFA, displays the step-by-step conversion process, and renders the final automaton graphically.
